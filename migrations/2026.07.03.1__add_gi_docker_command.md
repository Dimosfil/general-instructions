# Add GI Docker Command

Migration ID: `2026.07.03.1__add_gi_docker_command`

## Summary

Add `gi docker` / `ги докер` as a routed Docker/Compose runtime command for the
current project.

## Scope

- `AGENTS.md`
- `COMMANDS.md`
- `CHANGELOG.md`
- `VERSION.md`
- `patterns/AGENTS_RUNTIME/09-project-operation-commands.md`
- `templates/AGENTS.template.md`
- `tools/project-memory/instruction-kit.json`
- `templates/instruction-kit.template.json`

## Accepted Guidance

- Treat `gi docker` / `ги докер` as a request to restart the current project's
  documented Docker or Docker Compose runtime.
- Read project-local Docker/run instructions, compose files, Dockerfile or
  Containerfile, scripts, manifests, service records, and health-check
  contracts before touching containers.
- If no Docker/Compose config or documented Docker run contract exists, report
  that Docker is not configured for the project and stop.
- If Docker CLI, Docker Compose, or the Docker engine is missing or unavailable,
  report that blocker instead of claiming a restart.
- Rebuild before restart when the image is missing, build inputs changed, the
  local contract requires rebuild, or image freshness cannot be confidently
  proven.
- Scope operations to the current project only. Do not prune Docker state,
  remove volumes/images, or stop unrelated containers.
- Verify container status, health checks, mapped URLs, and relevant recent logs
  before reporting rebuilt/restarted/not-configured/blocked status.

## Verification

- Confirm `gi docker` / `ги докер` appears in `COMMANDS.md`.
- Confirm `AGENTS.md` and `templates/AGENTS.template.md` route the command to
  `patterns/AGENTS_RUNTIME/09-project-operation-commands.md`.
- Confirm the runtime module defines rebuild/restart decision rules and Docker
  absence handling.
- Run `git diff --check`.
- Confirm the new migration ID is listed in both instruction-kit metadata files.

## Rollback

Remove this migration ID from instruction-kit metadata, revert the version and
changelog entry, and remove only the `gi docker` command entries from the
entrypoint, command index, runtime module, and template files.

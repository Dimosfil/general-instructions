# Add Instruction Kit Migrations

Migration ID: `2026.05.16.3__add_instruction_kit_migrations`

## Purpose

Add migration-style instruction-kit updates for projects that copied this shared
instruction library.

## Shared Source Files

- `patterns/INSTRUCTION_KIT_MIGRATIONS.md`
- `templates/check-instruction-kit-updates.template.ps1`
- `migrations/README.md`

## Project Files

- `tools/check-instruction-kit-updates.ps1`
- `tools/project-memory/instruction-kit.json`

## Apply

- Create `tools/check-instruction-kit-updates.ps1` from the template if it is
  missing.
- Add or preserve `applied_migrations` in
  `tools/project-memory/instruction-kit.json`.
- Keep `updates/` out of the consuming-project update workflow.
- Use `VERSION.md`, `CHANGELOG.md`, `INDEX.md`, and `migrations/` as accepted
  update sources.

## Verify

- Run `tools/check-instruction-kit-updates.ps1`.
- Confirm pending migrations are listed without reading `updates/`.
- After the agent applies migration file changes, record applied migration IDs
  in `tools/project-memory/instruction-kit.json`.

# Add GI Restore Context Aliases

Migration ID: `2026.05.18.9__add_gi_restore_context_aliases`

## Purpose

Add short `gi` commands for restoring project context after opening a new chat.

## Shared Source Files

- `COMMANDS.md`
- `patterns/FIRST_MESSAGE_HANDLING.md`

## Project Files

- Root `AGENTS.md` or project working agreements may mention these aliases when
  documenting startup behavior.

## Apply

- Treat `gi старт` and `gi restore` as short aliases for project context
  restoration.
- Also accept `gi start`, `gi восстанови`, and
  `gi восстановить контекст`.
- On these commands, stay in the current project root and restore context from
  `AGENTS.md`, the latest handoff summary, and `tools/agent-start.ps1`.
- After restoring context, briefly report current status and ask what to do
  next.
- Do not automatically continue an old product task after context restore.

## Verify

- Confirm `gi старт` and `gi restore` are listed in `COMMANDS.md`.
- Confirm first-message handling treats these commands as restore-only startup
  commands.

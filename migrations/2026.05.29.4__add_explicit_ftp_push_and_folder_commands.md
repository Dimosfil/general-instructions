# Add Explicit FTP Push And Folder Commands

Migration ID: `2026.05.29.4__add_explicit_ftp_push_and_folder_commands`

## Purpose

Make FTP upload and remote folder selection explicit commands.

## Shared Source Files

- `AGENTS.md`
- `COMMANDS.md`
- `CHANGELOG.md`
- `VERSION.md`
- `patterns/PROJECT_FTP_DEPLOY.md`
- `templates/AGENTS.template.md`
- `migrations/2026.05.29.4__add_explicit_ftp_push_and_folder_commands.md`

## Project Files

- `AGENTS.md`
- copied command reference, if present
- project-local FTP config, commonly `tools/deploy/ftp.local.json`

## Apply

- Treat `gi ftp push` and `ги фтп пуш` as explicit requests to upload the
  configured build output to FTP, FTPS, or SFTP.
- Keep `gi ftp` and `ги фтп` as shorter upload aliases.
- Treat `gi ftp folder`, `gi ftp папка`, and `ги фтп папка` as requests to
  inspect, choose, or update the remote upload folder (`remotePath`) without
  uploading.
- When credentials and a selected FTP service are available, `gi ftp folder` may
  list remote directories and ask the user to choose with numbered Markdown
  checkboxes.
- When listing is unavailable, ask for the destination path and save it in the
  project-local FTP config.

## Verification

- Run `git diff --check`.
- Confirm `gi ftp push` uploads and `gi ftp folder` does not upload.
- Confirm `gi ftp folder` updates only `remotePath` unless the user explicitly
  asks to change other FTP settings.

## Rollback Or Conflict Notes

If a project already uses a different deploy vocabulary, preserve local command
aliases while mapping `gi ftp push` to upload and `gi ftp folder` to remote path
selection.

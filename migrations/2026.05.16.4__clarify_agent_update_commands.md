# Clarify Agent Update Commands

Migration ID: `2026.05.16.4__clarify_agent_update_commands`

## Purpose

Make shared-instruction update commands short, user-facing, and idempotent.

## Shared Source Files

- `COMMANDS.md`
- `patterns/INSTRUCTION_KIT_MIGRATIONS.md`
- `patterns/SHARED_INSTRUCTIONS_BOOTSTRAP.md`
- `patterns/GIT_WORKFLOW.md`
- `templates/AGENTS.template.md`
- `templates/AGENT_WORKING_AGREEMENTS.template.md`
- `checklists/NEW_PROJECT_AGENT_SETUP.md`

## Project Files

- `AGENTS.md`
- `tools/AGENT_WORKING_AGREEMENTS.md`
- `tools/project-memory/git-preferences.json`
- `tools/project-memory/instruction-kit.json`

## Apply

- Treat `Обновись из D:\AI\general-instructions\` as an idempotent command:
  bootstrap/init first when `tools/project-memory/instruction-kit.json` is
  missing, otherwise apply only pending migrations.
- Keep update checks limited to accepted release artifacts: `VERSION.md`,
  `CHANGELOG.md`, `INDEX.md`, and `migrations/`.
- Do not read `updates/` during consuming-project startup, bootstrap, or
  instruction-kit update checks.
- Keep default commit-message language preferences as English with no
  additional languages during bootstrap.
- When the user asks in chat to change commit-message languages, update
  `tools/project-memory/git-preferences.json` directly and summarize the new
  setting. Do not answer only with a PowerShell command.
- Preserve project-specific `AGENTS.md` and working-agreement content while
  merging these rules.

## Verify

- Run `tools/check-instruction-kit-updates.ps1` before and after applying this
  migration.
- Confirm this migration is recorded in
  `tools/project-memory/instruction-kit.json`.
- Confirm the project can use `Обновись из general-instructions` without
  prompting for commit-message languages.

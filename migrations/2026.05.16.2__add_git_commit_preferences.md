# Add Git Commit Preferences

Migration ID: `2026.05.16.2__add_git_commit_preferences`

## Purpose

Add reusable Git policy and project-local commit-message language preferences.

## Shared Source Files

- `patterns/GIT_WORKFLOW.md`
- `templates/git-preferences.template.json`
- `templates/select-git-commit-languages.template.ps1`

## Project Files

- `tools/project-memory/git-preferences.json`
- `tools/select-git-commit-languages.ps1`
- optional references in `AGENTS.md` or `tools/AGENT_WORKING_AGREEMENTS.md`

## Apply

- Create `tools/project-memory/git-preferences.json` from the template if it is
  missing.
- Create `tools/select-git-commit-languages.ps1` from the template if it is
  missing.
- Preserve existing project-specific Git policy.
- Ensure the default remains: the agent commits only when explicitly requested.
- Use English as the primary commit-message language and store selected
  additional languages in project memory.

## Verify

- Run the selector once or confirm the default file is valid JSON.
- Confirm the startup script reports Git commit language preferences when the
  file exists.

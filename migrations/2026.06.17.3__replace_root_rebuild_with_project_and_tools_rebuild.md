# Replace root rebuild with project and tools rebuild

## Summary

Replace the active `root rebuild` command wording with a clearer split:
`gi rebuild` rebuilds only the current project/application, while
`gi tools rebuild` and `gi rag rebuild` rebuild only the configured GI/RAG
tooling layer.

## Instructions

- Treat `gi rebuild` and `ги ребилд` as requests to rebuild the current project:
  the project/application build output such as an executable, package, or other
  documented artifact.
- Read project-local build or rebuild instructions, manifests, scripts, and
  packaging metadata before running the documented command.
- Do not treat `gi rebuild` as dependency restore, tests-only verification, a
  RAG-only rebuild, or a combined project-plus-RAG rebuild.
- Do not rebuild GI/RAG indexes or tools during `gi rebuild` unless the user
  explicitly asks for both project and RAG rebuilds.
- If no project rebuild contract exists, ask one short clarification question
  instead of inventing a command.
- Treat `gi tools rebuild`, `gi rag rebuild`, `ги тулс ребилд`, and
  `ги раг ребилд` as the active full GI/RAG rebuild commands.
- Treat node forms such as `gi tools rebuild sql`, `gi rag rebuild chunks`,
  `gi tools rebuild vector`, `gi rag rebuild manifest`, `gi tools rebuild evals`,
  and equivalent Russian forms as scoped GI/RAG node rebuild commands.
- Remove `gi root rebuild`, `ги рут ребилд`, and their node forms from active
  command lists and current agent rules.
- Keep `gi sql`, `gi sqlite`, and `gi vector` as inspection-only commands.

## Verify

- Confirm `COMMANDS.md` lists `gi rebuild`, `gi tools rebuild`, and
  `gi rag rebuild`, but does not list `root rebuild` as an active command.
- Confirm `AGENTS.md` and `templates/AGENTS.template.md` describe the project
  rebuild and GI/RAG tools rebuild distinction, with `gi rebuild` limited to the
  project/application build.
- Confirm `templates/rag-system.template.json` uses `gi tools rebuild` as the
  RAG full rebuild command.
- Confirm inspection commands are unchanged.

## Rollback Or Conflict Notes

If a consuming project already has a local `gi rebuild` meaning, project-local
instructions may override this shared rule, but the local command must still
document whether GI/RAG rebuild is included.

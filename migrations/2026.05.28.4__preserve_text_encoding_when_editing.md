# Preserve text encoding when editing

Migration ID: `2026.05.28.4__preserve_text_encoding_when_editing`

## Purpose

Prevent agents from corrupting non-ASCII source text by rewriting files through
PowerShell commands that use implicit or mismatched encodings.

## Shared Source Files

- `AGENTS.md`
- `CHANGELOG.md`
- `VERSION.md`
- `GENERAL_DEVELOPMENT_PLAYBOOK.md`
- `templates/AGENTS.template.md`
- `templates/instruction-kit.template.json`
- `migrations/2026.05.28.4__preserve_text_encoding_when_editing.md`

## Project Files

- Project-local `AGENTS.md`
- Project runbooks or working agreements that describe editing commands

## Problem

PowerShell whole-file rewrites can change or misread encodings when commands
such as `Get-Content ... | Set-Content ...` omit explicit encoding settings.
This can turn valid non-ASCII text into mojibake and mix unrelated encoding
damage into otherwise small source changes.

## Apply

- Add an encoding-safety rule to project agent instructions.
- Tell agents to preserve existing file encodings.
- On Windows, forbid source-file rewrites through PowerShell
  `Get-Content ... | Set-Content ...` pipelines unless both read and write
  encodings are explicit and known correct.
- Prefer `apply_patch`, editor-native saves, or language APIs that read and
  write with an explicit encoding such as UTF-8.
- If non-ASCII text appears as mojibake after a command, stop, restore the last
  clean file version, and reapply only the intended small patch.

## Verification

- Reread the updated project instruction file.
- Confirm the new rule is near the existing editing, tool, or worktree rules.
- Run `git diff --check`.

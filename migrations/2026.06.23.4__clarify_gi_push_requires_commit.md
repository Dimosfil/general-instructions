# Clarify GI Push Requires Commit

Migration ID: `2026.06.23.4__clarify_gi_push_requires_commit`

## Purpose

Prevent agents from treating `gi push` / `gi пуш` as a raw push-only git
operation when the user asked for the GI finish workflow.

## Shared Source Files

- `COMMANDS.md`
- `patterns/GIT_WORKFLOW.md`
- `templates/AGENT_WORKING_AGREEMENTS.template.md`
- `templates/instruction-kit.template.json`
- `tools/project-memory/instruction-kit.json`
- `VERSION.md`
- `CHANGELOG.md`

## Project Files

- `tools/AGENT_WORKING_AGREEMENTS.md`
- `tools/project-memory/instruction-kit.json`

## Apply

- Treat `gi push`, `gi пуш`, `gi commit push`, and `gi коммит пуш` as
  commit-then-push requests for scoped current changes.
- Do not reinterpret `gi push` / `gi пуш` as a raw `git push`, a retry of a
  previous terminal push, or a push-only command.
- Reserve push-only behavior for `gi only push` / `gi только пуш`.
- If `gi push` / `gi пуш` has no scoped changes to commit, report that clearly
  instead of running a push-only fallback.

## Verify

- Ask `gi пуш` with scoped uncommitted changes and confirm the agent stages only
  those changes, creates a commit, then pushes the current branch.
- Ask `gi пуш` when there are no scoped changes and confirm the agent reports
  that there is nothing scoped to commit instead of running a push-only fallback.
- Ask `gi только пуш` and confirm the agent does not stage files or create a
  commit, and only pushes existing local commits.

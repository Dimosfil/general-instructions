# Add GI First Test Command

Migration ID: `2026.06.16.6__add_gi_first_test_command`

## Summary

Add `gi first test`, `gi первый тест`, and `ги первый тест` as commands for
verifying the current application's first-launch experience after clearing its
documented first-run cache/state.

## Scope

- `AGENTS.md`
- `COMMANDS.md`
- `templates/AGENTS.template.md`
- `templates/AGENT_WORKING_AGREEMENTS.template.md`
- `tools/project-memory/instruction-kit.json`
- `templates/instruction-kit.template.json`
- `VERSION.md`
- `CHANGELOG.md`

## Accepted Guidance

- Treat `gi first test`, `gi первый тест`, and `ги первый тест` as first-launch
  verification requests.
- Read project-local run, cleanup, cache reset, and test instructions before
  clearing anything.
- Reset only documented project-owned app cache, generated state, temporary
  first-run profiles, and rebuildable local app settings.
- Do not delete user documents, production data, secrets, credentials, external
  service data, shared system caches, sibling projects, or arbitrary user-home
  folders.
- If exact reset paths, keys, scripts, or commands are missing, ask one concise
  clarification question instead of guessing.
- After reset, start the app, run the documented first-launch smoke or
  onboarding checks, and report what was cleared, what passed, and what was
  intentionally left untouched.

## Verification

- Ask `gi first test` in a project with a documented cache reset script; confirm
  the agent runs the documented reset, starts the app, and verifies first launch.
- Ask `gi первый тест` where reset paths are undocumented; confirm the agent
  asks one concise clarification question instead of deleting guessed folders.
- Ask `ги первый тест` in a project with user documents or production data near
  the app state; confirm those paths are not removed.
- Run `git diff --check`.

{
  "version": 1,
  "managers": [
    {
      "id": "worknest",
      "enabled": false,
      "base_url": "TODO",
      "workspace": "TODO",
      "project": "TODO",
      "intake_mode": "raw",
      "notes": "Store project-local non-secret task-manager preferences here. base_url is the manager API endpoint, not a UI URL unless the adapter explicitly says the same origin serves both UI and API."
    }
  ]
}
